class Solution {
public:
    string reverseWords(string s) {
        reverse(s.begin(),s.end());
       stringstream ss(s);
       string word;
       s="";
       while(ss>>word){
            reverse(word.begin(),word.end());
            s+=word+" ";
       }
       s.pop_back();
       return s;
    }
};
